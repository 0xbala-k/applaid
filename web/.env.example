# Copy to .env.local and fill in. Used by Next.js when running from web/ (e.g. npm run dev).

# Demo: trigger worker from dashboard. Requires worker running with WORKER_TRIGGER_PORT=3199
WORKER_TRIGGER_URL="http://localhost:3199"

{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "apply:once": "ts-node src/apply_and_update.ts",
    "seed:test-apply": "ts-node scripts/create-test-apply-task.ts",
    "db:clear": "ts-node scripts/clear-db.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.9.0",
    "dotenv": "^16.3.1",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/node-cron": "^3.0.11",
    "ts-node": "^10.9.1",
    "typescript": "^5.4.0",
    "vitest": "^4.0.18"
  }
}
